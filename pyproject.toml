[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "toy-model"
version = "0.1.0"
description = "Toy experiments for understanding deep learning and robot learning"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
